# XGrammar 进一步性能优化计划

## 目标说明

以历史统一重评中最快的 XGrammar 组合提交 `a831aa56112a697111dac8af31cab3235e5b392d` 为性能起点，在修复其已知语义差异的前提下继续优化，使 XGrammar 在固定 Modal 云端中央处理器环境的六类指定工作负载中，编译、允许词元集合生成和端到端总耗时都明确优于 llguidance，并留下可以独立复现的实现、测试、原始结果和中文报告。

## 验收标准

- AC-1：基线和候选边界可复现。
  - 正向测试：独立 worktree 的初始提交精确为 `a831aa56112a697111dac8af31cab3235e5b392d`；基线、候选和 llguidance 的源码或软件包版本、构建参数、哈希均有记录。
  - 反向测试：若实际加载路径不是指定非可编辑安装，或版本身份无法从结果中核对，则该轮结果无效。
- AC-2：六类工作负载全部覆盖。
  - 正向测试：小型/大型 JSON Schema、小型/大型 Structural Tag、完整工具调用基准、完整 JSON Schema 基准均有原始逐样例记录。
  - 反向测试：任何一类缺失、被较小子集代替或只报告聚合值，均不算完成。
- AC-3：三个时间指标全部覆盖且定义一致。
  - 正向测试：每类均报告编译时间、每次允许词元集合生成时间和端到端时间（编译加所有允许集合生成），并保留调用次数和统计方法。
  - 反向测试：把动态编译时首次生成允许集合的成本遗漏、只比较编译或只比较平均允许集合时间，均不算完成。
- AC-4：候选在指定矩阵上明确超过 llguidance。
  - 正向测试：同一输入、词表、路径、硬件与轮次下，候选在六类工作负载的三项指标均快于 llguidance；多轮方向和效应量足以排除同版本抖动。
  - 反向测试：仅端到端几何平均更快、个别类别更快、路径不同或落在控制波动内，均不能支持“全面超过”。
- AC-5：语义与稳定性没有已知回归。
  - 正向测试：相对正确主线基线，完整允许词元集合逐步差分为零；选定有效/无效文本结论一致；急切与动态模式差分、Python 全量、优化构建 C++、并发/序列化/缓存边界专项均通过。
  - 反向测试：历史 `Github_hard---o13152.json` 差异仍存在、崩溃、数据竞争、路径接受不一致或跳过失败样例时，候选不得进入最终结论。
- AC-6：优化确有增益且可维护。
  - 正向测试：候选相对 `a831aa56` 和正确性修正版都做同机交错对照；代码审查没有未解决的高风险问题；每项改动有对应测试。
  - 反向测试：收益只来自输入缩减、缓存预热泄漏、不同进程条件、错误路径或无测试快路时，改动应回退或重做。
- AC-7：产物与汇总完整。
  - 正向测试：任务目录包含脚本、worktree、原始结果、分析结果、`report.md` 与最终复现命令；报告总结会随新结论更新。
  - 反向测试：只给口头结论、缺少 Modal 原始结果或无法从脚本重算表格时，不算完成。

## 范围边界

### 最大范围

允许修改 XGrammar 的编译、缓存、解析状态、允许集合生成和 Python/C++ 接口内部实现；允许补充专项和差分测试；允许更新本任务内的 Modal 测量与分析脚本。只要对六类目标任务有真实收益，也可组合多个独立优化。

### 最小范围

最终至少需要一个相对正确性修正版有可重复提升的实现，并满足全部六类工作负载、三类时间和正确性验收；不能用报告或基准脚本变化替代产品实现性能提升。

### 可选与禁止项

- 可以：本地短测筛选、性能剖析、缓存、数据布局与算法优化、将逻辑拆成独立提交。
- 必须：正式计时只使用 Modal；Python 使用指向 worktree 源码构建的非可编辑安装。
- 禁止：直接修改主仓库；牺牲语义正确性换性能；改变输入或计时边界制造表面收益；把历史已知错误提交当正确性标准。

## 依赖与顺序

1. 建立基线：冻结提交、输入、软件版本、硬件与统计口径。
2. 恢复正确性：移植或重做已知重复范围和 JSON Schema 缓存修复，证明完整允许集合等价。
3. 性能诊断：用 Modal 基线和剖析数据定位未超过 llguidance 的类别与指标。
4. 迭代优化：每轮先本地正确性预检，再做 Modal 交错筛选，保留明确改善的改动。
5. 全量验收：六类负载三项指标、多轮控制、完整正确性和全量测试。
6. 独立审查与报告：处理审查问题，归档原始产物、分析与中文总结。

## 实现说明

产品代码和测试中不使用本计划的验收编号或里程碑术语。性能结论以原始数据自动分析为准，不以单轮或本机时间为准。
